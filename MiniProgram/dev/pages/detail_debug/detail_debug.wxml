<view class="title">
    <towxml nodes="{{detail.title}}"/>
    <view wx:if="{{ attr.title }}" id="title" class="edit" bind:tap="onEdit">edit</view>
    <towxml class="chinese" nodes="{{detail.chinese}}"/>
</view>

<towxml nodes="{{ detail.content }}"/>
<view wx:if="{{ attr.content }}" id="content" class="edit" bind:tap="onEdit">edit</view>
<towxml nodes="{{ detail.preuve }}"/>
<view wx:if="{{ attr.preuve }}" id="preuve" class="edit" bind:tap="onEdit">edit</view>
<towxml nodes="{{ detail.remarque }}"/>
<view wx:if="{{ attr.remarque }}" id="remarque" class="edit"  bind:tap="onEdit">edit</view>
<towxml nodes="{{ detail.exemple }}"/>
<view wx:if="{{ attr.exemple }}" id="exemple" class="edit" bind:tap="onEdit">edit</view>
<view class="source">
    <towxml nodes="{{ detail.source }}"/>
</view>
<view wx:if="{{ attr.source }}" id="source" class="edit" bind:tap="onEdit">edit</view>
